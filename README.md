# Download and setup

```bash
git clone https://github.com/mregrock/disk_fail_simulation.git
git submodule update --init --recursive
```

# Build and run

```bash
chmod +x build.sh
chmod +x run.sh
./build.sh && ./run.sh
```
